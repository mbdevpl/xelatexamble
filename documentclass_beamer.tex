
% beamer loads package xcolor, so any options used there should be added to \documentclass
% option ``envcountsect'' is for numbering theorems and definitions depending on section number 
\documentclass[envcountsect,CJK,10pt,compress,
  c, % vertical placement of conent on slides; one of: t (top), b (bottom), c (center)
  %draft, % uncomment to hide navigation headers/footers
  %handout, % uncomment to deactivate multi-stage slides
  xcolor={usenames,dvipsnames,svgnames,table,x11names},
  hyperref={unicode,pdfencoding=auto}
  ]{beamer}

% for including relative files within relatively included files
\usepackage{import}
