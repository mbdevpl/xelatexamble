
% remove vertical space in itemize with: [noitemsep,nolistsep]
\usepackage{enumitem}

% set margins using option [margin=1.5cm]
% and optionally change each margin individually
\usepackage{geometry}

% include this package as late as possible
% hyperlinks via \href{url}{link text}
% also, custom cross-referencing via \hyperref{label} or \hyperref[label]{link text}
\usepackage[unicode,pdfencoding=auto]{hyperref}
% clickable table of contents, package above also needed
\hypersetup{
    colorlinks,
    citecolor=black,
    filecolor=black,
    linkcolor=black,
    urlcolor=black
}
